# 学习记录 — 2026-05-24

## 环境配置

### 1. Zed 远程开发
- Windows 安装 Zed，连接 Linux 服务器（192.168.0.105）
- 配置 SSH 免密登录
- Zed settings.json 位置：`%APPDATA%\Zed\settings.json`
- 配置 `ssh_connections` 实现一键打开远程项目
- 常用快捷键：`Ctrl+Shift+P` 打开命令面板，`Open Remote` 连接远程服务器

### 2. SSH 配置
- Linux 端已配置长连接保活（ClientAliveInterval 60s）
- SSH 密钥认证已启用
- 配置文件：`~/.ssh/config`、`/etc/ssh/sshd_config.d/99-zed-dev.conf`

### 3. GitHub 配置
- 注册账号：fengjinhua6
- 配置 SSH 公钥认证
- 创建远程仓库：https://github.com/fengjinhua6/quant

---

## vnpy 量化框架学习

### 安装过程
- 源码位置：`/home/feng/vnpy_01`（已删除）
- 使用 Python 虚拟环境安装
- 安装依赖：ta-lib、pandas、plotly、pyzmq、tqdm 等
- 核心模块：trader（交易核心）、event（事件引擎）、alpha（因子研究）
- 数据库：vnpy_sqlite（SQLite 存储 K 线数据）

### CTA 回测
- 安装 `vnpy_ctastrategy` 策略模块
- 内置策略：双均线、布林带、ATR+RSI、海龟等
- 回测流程：准备数据 → 设置参数 → 加载策略 → 运行回测 → 查看统计

### 数据获取
- 使用 `efinance` 库下载期货行情数据
- 期货代码格式：上期所白银 `ag2606`，行情ID `113.ag2606`
- 成功下载 AG2606 日线数据（227 条）
- 双均线策略回测结果：14.43% 收益率

---

## Git / GitHub 基础操作

### 日常开发命令
```bash
git status              # 查看当前状态
git add 文件名           # 暂存文件
git commit -m "说明"     # 提交（拍快照）
git push                # 推送到 GitHub
git pull                # 拉取远程更新
git log                 # 查看提交历史
```

### 初始化新项目
```bash
mkdir 项目名
cd 项目名
git init
git branch -m main
git remote add origin git@github.com:用户名/仓库名.git
# 创建文件 → git add → git commit → git push -u origin main
```

### SSH 密钥配置
```bash
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_github
cat ~/.ssh/id_ed25519_github.pub  # 复制添加到 GitHub
```

---

## 常用快捷键

| 操作 | 快捷键 |
|------|--------|
| Zed 命令面板 | `Ctrl+Shift+P` |
| 打开远程项目 | `Ctrl+Shift+P` → `Open Remote` |
| 保存文件 | `Ctrl+S` |
| 打开终端 | `` Ctrl+` `` |

---

## 备注
- 配置文件：Linux 上 `~/.config/zed/settings.json`，Windows 上 `%APPDATA%\Zed\settings.json`
- JSON 文件中的键名和字符串必须用**直双引号** `"`，不能用中文引号
- OpenCode 历史记录按工作目录组织
